package trabajo_Practico2_Progra3;

import java.util.ArrayList;
import java.util.HashMap;

import org.openstreetmap.gui.jmapviewer.Coordinate;
import org.openstreetmap.gui.jmapviewer.MapMarkerDot;
import org.openstreetmap.gui.jmapviewer.MapPolygonImpl;
import org.openstreetmap.gui.jmapviewer.interfaces.MapPolygon;


public class logicaLocalidades {
	
	
		
	

	//CREA UN HASHMAP QUE SERIA (NOMBRE DEL HASHMAP (CLAVE) - UN ARRAYLIST(VALOR)) 
	public void crearListaCoordenada(String nombre, HashMap<String, ArrayList<Coordinate>> coordenadas) {
		coordenadas.put(nombre, new ArrayList<>());
		
	}
	
	
	
	//AL HASHMAP (CLAVE - VALOR), LE AGREGA UN VALOR DE TIPO COORDENADA AL ARRAYLIST
	public void agregarCoordenadaAListaCoordenada(String nombre, HashMap<String, ArrayList<Coordinate>> coordenadas, int x, int y) {
//		coordenadas.get(nombre).add(crearCoordenada(x, y));
		
		coordenadas.get(nombre).add(crearCoordenada(-34, -59));
		coordenadas.get(nombre).add(crearCoordenada(-35, -60));
		coordenadas.get(nombre).add(crearCoordenada(-36, -61));
		coordenadas.get(nombre).add(crearCoordenada(-40, -62));
		coordenadas.get(nombre).add(crearCoordenada(-38, -67));
	}
	
	
	public MapPolygon crearPoligono(String nombre, HashMap<String, ArrayList<Coordinate>> coordenadas) {
		MapPolygon poligono = new MapPolygonImpl(coordenadas.get(nombre));
		return poligono;
	}
	
	

	public Coordinate crearCoordenada(int x, int y) {
		Coordinate coordenada= new Coordinate(x, y);
		return coordenada;
	}
	

	
	
}
